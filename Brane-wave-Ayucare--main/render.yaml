services:
  - type: web
    name: ayucare-healthcare
    env: node
    buildCommand: npm install && npm run build
    startCommand: node server.js
    envVars:
      - key: SARVAM_API_KEY
        value:
